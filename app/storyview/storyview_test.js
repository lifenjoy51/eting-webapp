'use strict';

describe('myApp.storyview module', function() {

  beforeEach(module('myApp.storyview'));

  describe('storyview controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var storyviewCtrl = $controller('storyviewCtrl');
      expect(storyviewCtrl).toBeDefined();
    }));

  });
});
